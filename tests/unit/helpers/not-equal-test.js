import { notEqual } from 'dummy/helpers/not-equal';
import { module, test } from 'qunit';

module('Unit | Helper | not equal');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = notEqual([42]);
  assert.ok(result);
});
