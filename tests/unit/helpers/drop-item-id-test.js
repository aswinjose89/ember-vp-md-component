import { dropItemId } from 'dummy/helpers/drop-item-id';
import { module, test } from 'qunit';

module('Unit | Helper | drop item id');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dropItemId([42]);
  assert.ok(result);
});
