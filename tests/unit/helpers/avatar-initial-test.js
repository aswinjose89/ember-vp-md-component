import { avatarInitial } from 'dummy/helpers/avatar-initial';
import { module, test } from 'qunit';

module('Unit | Helper | avatar initial');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = avatarInitial([42]);
  assert.ok(result);
});
