import { sortItems } from 'dummy/helpers/sort-items';
import { module, test } from 'qunit';

module('Unit | Helper | sort items');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sortItems([42]);
  assert.ok(result);
});
