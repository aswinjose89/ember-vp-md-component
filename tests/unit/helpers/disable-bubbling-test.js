import { disableBubbling } from 'dummy/helpers/disable-bubbling';
import { module, test } from 'qunit';

module('Unit | Helper | disable bubbling');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = disableBubbling([42]);
  assert.ok(result);
});
