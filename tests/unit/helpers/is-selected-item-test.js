import { isSelectedItem } from 'dummy/helpers/is-selected-item';
import { module, test } from 'qunit';

module('Unit | Helper | is selected item');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isSelectedItem([42]);
  assert.ok(result);
});
