import { priorityConditionalClass } from 'dummy/helpers/priority-conditional-class';
import { module, test } from 'qunit';

module('Unit | Helper | priority conditional class');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = priorityConditionalClass([42]);
  assert.ok(result);
});
