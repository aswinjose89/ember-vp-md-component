import { emberPropertyValue } from 'dummy/helpers/ember-property-value';
import { module, test } from 'qunit';

module('Unit | Helper | ember property value');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = emberPropertyValue([42]);
  assert.ok(result);
});
