import truthConvert from 'dummy/utils/truth-convert';
import { module, test } from 'qunit';

module('Unit | Utility | truth convert');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = truthConvert();
  assert.ok(result);
});
